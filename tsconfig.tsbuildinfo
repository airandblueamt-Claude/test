import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./app/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        paper: '#FBFAF8',
        ink: '#161513',
        muted: '#6B6862',
        line: '#E7E3DB',
        // Functional tag palette — kept identical across the transcript,
        // the report, and the exported Word file so a colour always means
        // the same thing.
        tag: {
          question: '#E11D48', // rose  — auto-detected questions
          note: '#2563EB', // blue  — important notes
          requirement: '#7C3AED', // violet — requirements
          action: '#D97706', // amber — action items
          decision: '#059669', // emerald — decisions
        },
      },
      fontFamily: {
        sans: ['var(--font-plex-sans)', 'system-ui', 'sans-serif'],
        mono: ['var(--font-plex-mono)', 'ui-monospace', 'monospace'],
      },
    },
  },
  plugins: [],
};

export default config;
